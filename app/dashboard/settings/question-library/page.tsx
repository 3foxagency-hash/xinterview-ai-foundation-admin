import { SettingsPlaceholder } from '@/components/settings/settings-placeholder';

export default function QuestionLibraryPage() {
  return <SettingsPlaceholder title="Question library" scope="company" />;
}
