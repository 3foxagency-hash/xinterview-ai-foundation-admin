import { SettingsPlaceholder } from '@/components/settings/settings-placeholder';

export default function NotificationsPage() {
  return <SettingsPlaceholder title="Notifications" scope="personal" />;
}
