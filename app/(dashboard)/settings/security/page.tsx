import { SettingsPlaceholder } from '@/components/settings/settings-placeholder';

export default function SecurityPage() {
  return <SettingsPlaceholder title="Password & security" scope="personal"
      description="Your password, active sessions and two-factor authentication."
    />;
}
