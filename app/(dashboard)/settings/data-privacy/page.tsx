import { SettingsPlaceholder } from '@/components/settings/settings-placeholder';

export default function DataPrivacyPage() {
  return <SettingsPlaceholder title="Data & privacy" scope="company"
      description="Data retention, candidate consent and export or deletion requests."
    />;
}
