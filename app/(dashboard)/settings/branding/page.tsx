import { SettingsPlaceholder } from '@/components/settings/settings-placeholder';

export default function BrandingPage() {
  return <SettingsPlaceholder title="Branding" scope="company"
      description="Your company's visual identity. These apply as defaults across candidate-facing interview pages."
    />;
}
