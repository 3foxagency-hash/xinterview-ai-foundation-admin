import { SettingsPlaceholder } from '@/components/settings/settings-placeholder';

export default function IntegrationsPage() {
  return <SettingsPlaceholder title="Integrations" scope="company" />;
}
