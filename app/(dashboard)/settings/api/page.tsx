import { SettingsPlaceholder } from '@/components/settings/settings-placeholder';

export default function ApiPage() {
  return <SettingsPlaceholder title="API & webhooks" scope="company"
      description="API keys and webhook endpoints for building on top of XInterview."
    />;
}
