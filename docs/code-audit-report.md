# Dead Code Audit — xinterview admin

**Repo:** `xin - admin-new /repo` · **Branch:** `bolt-28-aug-2026` · **Commit:** `137747a` (baseline, clean)
**Date:** 2026-08-28 · **Auditor:** automated pass (knip, ts-prune, depcheck, madge, `next build`) + manual grep verification of every finding below.

> **Stack discrepancy note.** The audit brief described this stack as including TanStack Query, nuqs, MSW, Vitest, Playwright, and Sentry. None of those packages are present in `package.json`, and none are imported anywhere in the tree (verified by grep — zero hits for `useQuery`, `useTranslation`/i18n APIs, `msw`, `vitest`, `playwright`, `sentry`). This repo is a mock-data-driven admin app (`lib/api/*.ts` are documented in-memory stand-ins — see `docs/api-implementation-standard.md`), not yet wired to a real backend. Categories 8 (i18n keys) and the "query keys" half of category 7 are **N/A** for this codebase; everything else below is a real finding. I did not add or infer any of those missing packages — just report what's actually here.

---

## 1. Summary

| Metric | Value |
|---|---|
| `.ts`/`.tsx` files scanned | 277 |
| Baseline `next build` | ✅ green (before and after audit — audit made no source changes) |
| Total findings | 39 line items across 12 categories |
| SAFE (zero refs, no dynamic usage) | 15 |
| REVIEW (probably dead, has an edge case) | 16 |
| KEEP (tool false positive) | 8 |
| Estimated dependency savings | 18 npm packages removable outright (see §2.4); ~2 more (`@radix-ui/react-slider`, `@radix-ui/react-toggle`) tied to a component decision |
| Estimated disk savings | ~1.5 MB in `/public` (one orphaned PNG + one superseded probe file) + ~1,340 lines of dead hand-written TS in `lib/api/` (excludes the 19k-line generated schema, which is a build artifact, not a deletion candidate) |
| Tools requiring temporary install | None — `npx knip`, `npx ts-prune`, `npx depcheck`, `npx madge` all ran via npx's transient cache; `package.json`/`package-lock.json` were never modified. `npm install` was run once to materialize `node_modules` (gitignored, required for `next build` to run at all) and made no lockfile changes. |

---

## 2. Findings by category

### 2.1 Unused files (whole modules, zero importers)

Source: `npx knip` "Unused files" section, individually re-verified with `grep -rn` for each basename across the tree.

| File | Evidence | Risk | Recommendation |
|---|---|---|---|
| `lib/api/client.ts` | `grep -rn "from '.*client'" **/*.ts*` → 0 external importers. Doc comment inside the file: none of `app/`, `components/` import it. | REVIEW | Part of a coherent, documented future-backend cluster — see box below. Not accidental cruft. |
| `lib/api/contract.ts` | 0 importers outside the cluster itself (`errors.ts`, `client.ts` import it) | REVIEW | Same cluster. |
| `lib/api/errors.ts` | Only imported by `client.ts` (which is itself unused) | REVIEW | Same cluster. |
| `lib/api/rate-limit.ts` | Only imported by `client.ts` | REVIEW | Same cluster. |
| `lib/api/contract-drift.ts` | 0 importers anywhere | REVIEW | Same cluster. |
| `lib/api/user-management-contract.ts` | 0 importers anywhere | REVIEW | Same cluster. |
| `lib/config/env.ts` | Only imported by `client.ts` | REVIEW | Same cluster. |
| `lib/api/generated/schema.ts` | Only imported by `contract-drift.ts`; regenerated by `npm run api:types` from `backend-schema/xinterview-schema.yaml` | KEEP | OpenAPI-generated file — false-positive rule explicitly excludes these. |
| `components/ui/slider.tsx` | `grep -rn "components/ui/slider" --include=*.tsx` → 0 hits outside itself | KEEP | shadcn/ui primitive — library code, "not currently used" not "delete." |
| `components/ui/toggle.tsx` | `grep -rn "components/ui/toggle'" --include=*.tsx` → 0 hits outside itself | KEEP | Same — shadcn primitive. |

> **The `lib/api/client.ts` cluster, explained.** `docs/api-implementation-standard.md` (already in the repo, dated, "Status: Proposal for review") documents exactly this: the app currently mocks at the *function* level (`lib/api/jobs.ts`, `auth.ts`, etc. — all in-memory stores, actively used, see §2.2) and the doc proposes moving the mock seam to the *network* level (`useJob() → api.GET('/jobs/:id') → fetch`, mocked via a future interceptor). `client.ts` + `contract.ts` + `errors.ts` + `rate-limit.ts` + `contract-drift.ts` + `user-management-contract.ts` + `lib/config/env.ts` are the first bricks of that target architecture — a hand-written REST client, error taxonomy, rate-limit/circuit-breaker helper, and OpenAPI-drift checker — laid down ahead of the migration but not yet wired to any page. They are 100% inert today (confirmed: no file in `app/` or `components/` imports any of them, `next build` succeeds without them) but deleting them would work against a documented, in-flight architecture decision. **Flagged REVIEW, not SAFE** — a human who has read the proposal doc should decide whether to keep incubating this cluster or delete it and restart when the real backend lands.

### 2.2 Unused exports inside used files

Source: `npx ts-prune` + `npx knip` "Unused exports" / "Unused exported types," cross-checked by grepping each symbol name.

| File / Symbol | Evidence | Risk | Recommendation |
|---|---|---|---|
| `components/auth/index.ts` — 27 re-exports (`AuthCard`, `AuthButtonProps`, `VideoPanel`, `useOtpResend` types, etc.) | The barrel *is* imported (6 sites: `grep "from '@/components/auth'"`), but every call site imports only the handful of components it needs directly by path too (e.g. `import { RadioCardGroup } from '@/components/auth/radio-card-group'`), and the barrel import lines (`login/page.tsx`, `signup/page.tsx`, …) pull disjoint subsets. Several named re-exports (`VideoPanel`, most of the `*Props` types) are never the symbol actually consumed through the barrel. | REVIEW | Prune the barrel to only what's actually pulled through it, or delete it and have all 6 call sites import directly (they mostly already do for other symbols). |
| `components/settings/index.ts` — 11 re-exports | Every one of the 8 barrel-import call sites (`grep "from '@/components/settings'"`) imports only `SettingsPage`. `RoleBadge`/`StatusBadge` are used, but via **direct** file import (`components/settings/role-badge.tsx`) in `settings/team/page.tsx`, never via the barrel. `SegmentedControl`, `SettingsInput`, `SettingsSelect`, `SettingsSubNav` — same story (used directly elsewhere, dead through the barrel). | SAFE (barrel-only re-exports) | Barrel can be trimmed to `export { SettingsPage } from './settings-page'` with zero call-site changes. |
| `lib/api/candidates.ts`: `updateCandidateStage`, `candidateShareLink`, `_resetCandidateStore` | `grep -rn "updateCandidateStage\|candidateShareLink\|_resetCandidateStore"` → defined, never called | REVIEW | `_reset*` naming convention strongly implies a test-suite hook, but **no test files exist in this repo at all** (`find . -iname "*.test.*" -o -iname "*.spec.*"` → 0 results). Either the tests were removed, or written and never landed. |
| `lib/api/email-templates.ts`: `setEmailTemplateEnabled`, `_resetEmailTemplateStore` | Same pattern | REVIEW | Same reasoning. |
| `lib/api/profile.ts`: `_resetProfileStore` | Same pattern | REVIEW | Same reasoning. |
| `lib/api/settings.ts`: `_setSimulateSeatLimit`, `uploadLogo`, `getCareerPageId`, `_resetCareerStore`, `_resetIntegrationsStore`, `_resetSettingsStore` | Same pattern | REVIEW | Same reasoning — this is the largest single cluster of test-hook-shaped dead exports. |
| `lib/api/sms-templates.ts`: `setSmsTemplateEnabled`, `_resetSmsTemplateStore` | Same pattern | REVIEW | Same reasoning. |
| `lib/api/auth.ts`: `setSession`, `getProfile`, `logout` | `grep -rn "\bgetProfile\b\|\blogout\b"` outside the file → 0 | REVIEW | Plausible near-term needs (a logout button, a profile refetch) — verify with product before deleting; may be intentionally-ahead-of-UI. |
| `lib/api/jobs.ts`: `getPreviousJobTitles`, `getInviteReadiness`, `publishJob` | 0 external refs | REVIEW | Same — `jobs.ts` is heavily used elsewhere (19 importers), these three specific exports just aren't wired to any UI yet. |
| `lib/wizard-config.ts`: `getStepByNumber`, `getStepById` | 0 external refs | SAFE | Small, self-contained helpers; genuinely orphaned. |
| `lib/settings-nav-config.ts`: `ALL_SETTINGS_SECTIONS`, `SETTINGS_SECTION_TITLES` | 0 external refs | SAFE | Same. |
| `components/wizard/wizard-input.tsx`: `WizardSelect` | `grep -rn "WizardSelect"` → only its own definition | SAFE | Wraps `components/ui/select.tsx` (Radix) but is never instantiated; `CountryCodeSelect` and hand-rolled selects (§3) cover the actual select needs in the app today. |
| `lib/interview/use-connection-speed.ts`: `UPLOAD_ADEQUATE_MBPS` / `UPLOAD_REQUIRED_MBPS` flagged as "duplicate exports" by knip | `UPLOAD_REQUIRED_MBPS = UPLOAD_ADEQUATE_MBPS` (line 29) — an intentional alias, not a bug | KEEP | Not dead code, just two names for one constant. No action needed. |
| `lib/constants/company-types.ts`: `ACCOUNT_TYPE_OPTIONS` | 0 external refs | REVIEW | `AccountType` (the type) is used; the options array built from it isn't consumed anywhere yet. |
| `lib/validation/job.ts`: `questionsSchema`, `InviteRow`, `invitesSchema`, `LOCKED_STAGE_NAMES`, plus ~15 more schema/type exports | 0 external refs per ts-prune | REVIEW | This file is large (`lib/validation/job.ts`) and actively used for other schemas in the same file — these specific ones look like schemas prepared ahead of form wiring that hasn't landed. Worth a Slack to whoever owns the create-job wizard before pruning. |

*(Full remaining ts-prune/knip export list — mostly single-use internal types marked "used in module" by the tools themselves — is in the raw tool logs; only genuinely zero-reference, cross-file-dead exports are tabulated above to keep this actionable.)*

### 2.3 Unused hooks, utils, types, constants

| Item | Evidence | Risk | Recommendation |
|---|---|---|---|
| All 5 custom hooks (`useOtpResend`, `useSidebar`, `useCustomisationSave`, `useConnectionSpeed`, `useDetectedCountry`) | `grep -rl "\b<hook>\b" app components lib config` → each has 2–11 referencing files | KEEP | All actively used. No unused hooks found. |
| `lib/interview/theme.ts` internal color-math helpers (`luminance`, `contrast`, `hexToHsl`, etc.) | Flagged by knip as "unused export," but each is `(used in module)` per ts-prune — i.e. used internally, just not exported-and-consumed elsewhere | KEEP | False positive at the "should this be exported" level only; not dead logic. Could be un-exported (tighten the module's public surface) but that's a style nit, not a deletion. |
| `lib/constants/common-passwords.ts`, `lib/constants/disposable-domains.ts` | Both flagged "used in module" | KEEP | Actively used by password/email validation. |

### 2.4 Unused npm dependencies

Source: `npx knip` + `npx depcheck` (both agree), individually re-verified: for every flagged package I confirmed **no corresponding `components/ui/*.tsx` wrapper file exists at all** (the repo has only 16 files in `components/ui/`, not the ~30 a full shadcn install would generate) and zero direct imports anywhere.

| Dependency | Evidence | Risk | Recommendation |
|---|---|---|---|
| `@radix-ui/react-accordion`, `-alert-dialog`, `-aspect-ratio`, `-collapsible`, `-context-menu`, `-hover-card`, `-menubar`, `-navigation-menu`, `-progress`, `-radio-group`, `-scroll-area`, `-separator`, `-tabs`, `-toggle-group` (14 packages) | `grep -rl "from '@radix-ui/react-<name>'"` → 0 hits; no `components/ui/<name>.tsx` file exists | SAFE | Remove from `dependencies`. |
| `cmdk`, `embla-carousel-react`, `react-resizable-panels`, `vaul` (4 packages) | Same — no `command.tsx`/`carousel.tsx`/`resizable.tsx`/`drawer.tsx` wrapper exists, 0 imports | SAFE | Remove. |
| `pngjs` (devDependency) | `grep -rn "pngjs"` across all `.ts/.tsx/.js` → 0 hits; no build script references it | SAFE | Remove. |
| `@radix-ui/react-slider` | Only importer is `components/ui/slider.tsx`, which is itself unused (§2.1) | REVIEW | Tied to the `slider.tsx` decision — if the shadcn primitive stays (KEEP per library rule), the dep stays too. |
| `@radix-ui/react-toggle` | Only importer is `components/ui/toggle.tsx`, itself unused (§2.1) | REVIEW | Same. |
| `@netlify/plugin-nextjs` | Flagged "unused" by both knip and depcheck (neither scans `.toml`) — but `netlify.toml` line 6 declares `package = "@netlify/plugin-nextjs"` under `[[plugins]]` | **KEEP — false positive** | Referenced only in a config file, per the audit's own false-positive rule. Do not remove. |
| `eslint`, `eslint-config-next` | Flagged by knip — but `.eslintrc.json` is `{"extends": "next/core-web-vitals"}` and `npm run lint` invokes `next lint` | **KEEP — false positive** | Config-file-only usage. |
| `autoprefixer` | Flagged by depcheck — but `postcss.config.js` lists it as a plugin | **KEEP — false positive** | Config-file-only usage. |
| `@types/node`, `postcss` | Flagged by depcheck — types-only / build-toolchain packages never appear in source imports | **KEEP — false positive** | Standard tooling deps. |
| `date-fns` (missing, not unused) | depcheck: "Missing dependencies — date-fns: ./app/jobs/new/setup/page.tsx". Confirmed: `import { format as formatDate, isPast, isToday } from 'date-fns'` at `app/jobs/new/setup/page.tsx:36`, but `date-fns` is absent from `package.json`. It currently resolves only because `react-day-picker` pulls it in transitively (`node_modules/date-fns@3.6.0` present, un-pinned). | REVIEW (bonus finding, not "unused" but a related dependency-hygiene bug) | Add `date-fns` to `package.json` `dependencies` explicitly — a `react-day-picker` version bump that drops or changes its `date-fns` peer would silently break this page. |

### 2.5 Duplicate components

| Pair / Group | Evidence | Risk | Recommendation |
|---|---|---|---|
| `components/auth/auth-select.tsx` (`AuthSelect`) vs `components/settings/settings-select.tsx` (`SettingsSelect`) | Both hand-roll an identical custom listbox: same `useState`/`useRef` shape, same `open`/keyboard-nav logic (`Enter`/`Space`/`ArrowDown` to open, `Escape` to close-and-refocus), same `role="listbox"`/`role="option"` a11y pattern, same click-outside overlay `<div className="fixed inset-0 z-40">`. Diff is cosmetic (`h-12` vs `h-10`, `text-[13px]` vs `text-body-sm`) plus `AuthSelect` adds a `searchable` filter box. Neither uses `components/ui/select.tsx`, the Radix-based shadcn primitive already in the codebase and used in 8 other places. | REVIEW | Not a pure delete — both are live and used. **Merge plan:** extract one `BaseSelect` (or extend `components/ui/select.tsx` with a `searchable` prop) parameterized by size/token-set: `<Select variant="auth" searchable />` / `<Select variant="settings" />`. Saves ~130 duplicated lines and one less place for the listbox a11y logic to drift. |
| `delete-api-key-dialog.tsx`, `delete-company-dialog.tsx`, `delete-template-dialog.tsx`, `remove-member-dialog.tsx` (all in `components/settings/`), `candidates/delete-candidate-dialog.tsx` | `diff <(grep -oE 'className="[^"]*"' delete-api-key-dialog.tsx) <(grep -oE 'className="[^"]*"' delete-candidate-dialog.tsx)` → **empty diff** — byte-identical Tailwind class strings across two of the five files. All five share: `loading` state, `cancelRef` autofocus-on-open effect, identical Cancel/Delete button markup (`inline-flex h-9 items-center justify-center rounded-md border border-border-strong …` / `… bg-error px-4 text-button text-error-foreground …`), differing only in the title/description copy and the async action they call. | REVIEW | Not a pure delete — all five are live and used. **Merge plan:** extract a single `<ConfirmDestructiveDialog open onOpenChange title description confirmLabel onConfirm />` in `components/ui/` (or `components/settings/`), have all five call sites pass copy + handler. Saves ~350 duplicated lines and centralizes the focus-management a11y logic in one place instead of five. |
| `components/auth/auth-input.tsx` vs `components/settings/settings-input.tsx` | Same concept (labeled input + error state), but `auth-input.tsx` (96 lines) adds icon slots and differs structurally from `settings-input.tsx` (52 lines, adds a prefix-border slot instead). Not a near-identical duplicate like the two pairs above. | UNSURE | Flagging for awareness only — worth a look during the Select/Dialog consolidation pass, but the two components solve different enough problems (icon vs. prefix) that forcing a merge may not pay off. Human judgment call. |

### 2.6 Dead routes / orphan pages

Checked every `page.tsx` route segment against `lib/nav-config.ts` (sidebar), `lib/settings-nav-config.ts` / `components/sidebar/profile-menu.tsx` (account menu), and direct string/dynamic-link greps.

| Route | Evidence | Risk | Recommendation |
|---|---|---|---|
| All top-level dashboard routes (`/dashboard`, `/create-job`, `/jobs`, `/candidates`, `/reports`, `/workspace-settings`, `/documentation`, `/whats-new`, `/support`, `/notifications`) | Initially 0 hits on literal `href="/x"` grep — because they're wired through `lib/nav-config.ts`'s `primaryNavGroups`/`bottomNavItems` arrays, rendered by the sidebar via `item.href`, not literal strings at each call site. Confirmed by reading `lib/nav-config.ts`. | KEEP — false positive of naive grepping | All linked; initial 0-hit grep was a methodology trap, not a real finding. Included here so the false-positive is documented, not silently dropped. |
| `/company-setup` | `grep -rn "company-setup"` → `components/auth/verify-form.tsx:62`: `isSignup ? '/company-setup' : '/reset-password'` | KEEP | Linked from the signup verification flow. |
| `/share` | `grep -rn "/share"` across the whole tree (not just `app/`) → `app/(dashboard)/jobs/[id]/page.tsx:321` and `lib/workflow-mock.ts:777` both build `` `${origin}/share${query}` `` dynamically to hand to users as a copyable link; `lib/api/candidates.ts:145` builds a similar external share URL. | KEEP | Reached via generated/copied URLs, not in-app `<Link>` — exactly the "dynamic usage" false-positive case the brief calls out. Confirmed alive. |
| `app/(dashboard)/settings/page.tsx` (bare `/settings` — a `redirect('/settings/general')` stub) | `grep -rn "\"/settings\"\|'/settings'"` (excluding `/settings/*` and `/workspace-settings`) → 0 hits. The account/profile menu (`components/sidebar/profile-menu.tsx:90-92`) links directly to `/settings/profile`, `/settings/billing`, `/settings/general` — it bypasses the bare index entirely. | REVIEW | The index page is a Next.js convention file (`page.tsx`) serving as a URL-safety fallback for anyone who navigates to bare `/settings` directly (bookmark, typed URL, old link) — legitimate to keep even with 0 in-app inbound links. Flagging as REVIEW, not SAFE, per the convention-file false-positive rule; a human can decide the fallback isn't worth the route if they're confident nothing external links to bare `/settings`. |
| `app/api/speed-test/upload/route.ts` (+ its asset `public/speed-test/probe.bin`) | The **route's own doc comment is the smoking gun** — `components/interview/setup-screen.tsx:61-64`: *"The homemade single-fetch speed probes that lived here (a 256KB download from `/speed-test/probe.bin` and a 256KB POST to `/api/speed-test/upload`) have been replaced by `@cloudflare/speedtest` via `useConnectionSpeed`."* `grep -rn "speed-test/upload\|probe.bin"` → the only remaining hits are that one explanatory comment and the route file itself. `lib/interview/use-connection-speed.ts:102` confirms the replacement: `await import('@cloudflare/speedtest')`. | REVIEW (evidence is SAFE-strength, but it's a public HTTP endpoint) | In-repo evidence is unambiguous: this route and asset are superseded and dead. Flagged REVIEW rather than SAFE only because `route.ts` is a public network surface — confirm no external client (an old mobile build, a QA harness, a CDN health check) still POSTs to it before deleting. `probe.bin` (256 KB) can be deleted in the same batch as the route. |

### 2.7 Unused API client functions and query keys

- **Query keys:** N/A — no TanStack Query / SWR in this codebase (`grep -rln "queryKey\|useQuery\|useMutation"` → 0 hits).
- **Unused API client functions:** covered in §2.1 (the whole `client.ts`/`contract.ts` cluster) and §2.2 (individual dead exports inside the live `lib/api/*.ts` mock modules).

### 2.8 Unused i18n translation keys

N/A — no i18n framework installed (`grep -iE "intl|i18n|locale"` on `package.json` → 0 hits; no `next-intl`/`react-i18next`; no locale/translation JSON files found anywhere in the tree via `find . -iname "*i18n*" -o -iname "*locale*" -o -iname "*translation*"`). All UI copy is inline English strings. Nothing to audit in this category for this repo.

### 2.9 Unused env vars / undocumented env vars

| Var | Evidence | Risk | Recommendation |
|---|---|---|---|
| `NEXT_PUBLIC_APP_ENV`, `NEXT_PUBLIC_API_BASE_URL` | Both declared in `.env.example` and `.env.development`, both consumed in `lib/config/env.ts` (`process.env.NEXT_PUBLIC_APP_ENV`, `process.env.NEXT_PUBLIC_API_BASE_URL`) — a 1:1 match, no orphans, no undocumented vars. | KEEP | Documented and consistent. Note: the *consuming* module (`lib/config/env.ts`) is itself part of the dead `client.ts` cluster (§2.1) — so these vars are correctly declared but not currently exercised by any live code path. No action needed beyond the §2.1 decision. |

### 2.10 Commented-out code / TODO/FIXME

| Check | Evidence | Result |
|---|---|---|
| `TODO`/`FIXME`/`XXX:` markers | `grep -rn "TODO\|FIXME\|XXX:" --include=*.ts --include=*.tsx` (excluding node_modules/.next) | **0 hits.** Clean. |
| Commented-out code (lines starting `//` that look like `const`/`import`/`export`/JSX) | `grep -rnE "^\s*//\s*(const |let |import |export |function |return |if\s*\(|<[A-Za-z])"` | **1 hit**, and it's an explanatory comment (`components/interview/video-recorder.tsx:54` — `// <video> is already unmounted by the time an effect could read it.`), not disabled code. |
| Block comments (`/* … */`) | Spot-checked ~15 files with block comments (`app/(dashboard)/dashboard/page.tsx`, etc.) | All are prose documentation explaining *why*, not commented-out logic. |

No findings in this category — the codebase is unusually clean of TODO debt and commented-out code.

### 2.11 Unused assets in /public

| Asset | Evidence | Risk | Recommendation |
|---|---|---|---|
| `public/images/ui/f231f007-979b-4b15-8aa9-b3d1c54c4a33_(1).png` (1.2 MB) | `grep -rn "images/ui\|f231f007"` across all `.ts/.tsx/.css` → **0 hits anywhere.** No component, no CSS `url()`, no metadata reference. | SAFE | Delete. Largest single-file win in the audit. |
| `public/speed-test/probe.bin` (256 KB) | See §2.6 — superseded by `@cloudflare/speedtest`, only referenced in the explanatory comment describing its own retirement. | REVIEW | Delete together with the `route.ts` in the same batch (§2.6). |
| `public/CVArchanaGaitonde.pdf` | `lib/workflow-mock.ts:677-679` — `RESUME_URL` / `fileName` constants | KEEP | Used by mock candidate data. |
| `public/G2-badge-signin.6cb66346.svg` | `app/(auth)/login/page.tsx:82` | KEEP | Used. |
| `public/candidate-aarav-portrait.webp` | `components/interview/interview-parts.tsx:70` (video `poster`) | KEEP | Used. |
| `public/videos/intro-sample.mp4` | `config/interview.mock.ts:133`, `components/interview/dev-panel.tsx:44,70` | KEEP | Used (dev-only mock video, but genuinely referenced). |

### 2.12 Unused CSS / Tailwind custom classes

| Item | Evidence | Risk | Recommendation |
|---|---|---|---|
| `tailwind.config.ts` `keyframes`/`animation`: `accordion-down`, `accordion-up`, `fade-in`, `slide-in-right`, `float`, `float-delayed`, `gradient-shift`, `pulse-ring` (8 custom animations) | `grep -rl "animate-<name>"` for each of the 8 → **0 hits, every single one**, across `app/`, `components/`, `lib/`, `config/`. | SAFE | Remove all 8 keyframe/animation definitions from `tailwind.config.ts` (lines ~283–325). `accordion-down`/`accordion-up` are dead specifically because there's no Accordion component in this app at all (confirms §2.4 — `@radix-ui/react-accordion` was never wired up). |
| Custom color tokens `jobs`, `candidates`, `interviews`, `reports`, `settings`, `ai` | `grep -rlE "(bg|text|border|from|to|ring|fill|stroke)-<name>-"` → 2–5 files each use them | KEEP | All actively used domain-accent colors. |

---

## 3. Duplicate components — merge plan

Two genuine duplication clusters found (detailed with evidence in §2.5):

1. **`AuthSelect` + `SettingsSelect`** → consolidate into one parameterized select (extend `components/ui/select.tsx` or a shared `BaseSelect`), ~130 lines saved.
2. **Five destructive-confirm dialogs** (`delete-api-key-dialog`, `delete-company-dialog`, `delete-template-dialog`, `remove-member-dialog`, `delete-candidate-dialog`) → consolidate into one `<ConfirmDestructiveDialog>`, ~350 lines saved.

Both are REVIEW-risk merges (all five+two components are live and in active use) — they belong in their own refactor PR, separate from the deletion batches below, since they change behavior-carrying code rather than removing dead code.

---

## 4. Suggested deletion order (small PRs, safest first)

**Batch 1 — zero-risk assets & config (no code changes)**
- Delete `public/images/ui/f231f007-979b-4b15-8aa9-b3d1c54c4a33_(1).png`
- Remove 8 unused `animation`/`keyframes` entries from `tailwind.config.ts`
- Remove 18 SAFE unused npm packages from `package.json` (§2.4: 14 Radix packages, `cmdk`, `embla-carousel-react`, `react-resizable-panels`, `vaul`, `pngjs`) and run `npm install` to sync the lockfile
- *Do not* touch `@netlify/plugin-nextjs`, `eslint`, `eslint-config-next`, `autoprefixer`, `@types/node`, `postcss` (confirmed false positives, §2.4)

**Batch 2 — dead barrel re-exports**
- Trim `components/settings/index.ts` to only `export { SettingsPage } from './settings-page'`
- Trim `components/auth/index.ts` to only the symbols actually consumed through the barrel (verify each of the 6 call sites individually first)

**Batch 3 — orphaned speed-test leftovers**
- Delete `app/api/speed-test/upload/route.ts` and `public/speed-test/probe.bin`
- **Before merging:** confirm with whoever owns mobile/QA clients that nothing outside this repo POSTs to `/api/speed-test/upload`

**Batch 4 — small dead exports (helpers, constants)**
- `lib/wizard-config.ts`: remove `getStepByNumber`, `getStepById`
- `lib/settings-nav-config.ts`: remove `ALL_SETTINGS_SECTIONS`, `SETTINGS_SECTION_TITLES`
- `components/wizard/wizard-input.tsx`: remove `WizardSelect`
- Add `date-fns` explicitly to `package.json` `dependencies` (bug-fix, not a deletion — bundle it here since it's dependency-hygiene work)

**Batch 5 — largest, most judgment-heavy (human decision required first)**
- The `lib/api/client.ts` / `contract.ts` / `errors.ts` / `rate-limit.ts` / `contract-drift.ts` / `user-management-contract.ts` / `lib/config/env.ts` cluster (~740 hand-written lines) — **do not delete without reading `docs/api-implementation-standard.md` and asking the doc's author whether the real-backend migration is still planned.** If yes: keep as-is (it's inert, costs nothing but repo size). If no: delete the whole cluster together (they only reference each other) and regenerate `lib/api/generated/schema.ts`'s consumer later if a decision to migrate is made.
- The dozen-plus `_reset*Store` / `set*Enabled` test-hook-shaped exports across `lib/api/*.ts` (§2.2) — confirm with the team whether a test suite is planned (there is currently none in the repo) before removing; if tests are coming, these are the seams they'll attach to.

**Not a deletion batch — refactor PR:** the Select and ConfirmDialog consolidations from §3, done separately since they touch live, working UI.

---

## 5. Verification steps after each batch

1. `npm run typecheck` (`tsc --noEmit`)
2. `npm run lint` (`next lint`)
3. `npx next build` — must stay green, same route list as the baseline log (77 routes at time of audit)
4. Unit tests — **none exist in this repo currently**, so this step is a no-op until a suite is added; note it here so it isn't silently skipped once one exists
5. E2E tests — **none exist in this repo currently**, same note as above
6. For Batch 1 (dependency removal specifically): `git diff package.json package-lock.json` — confirm only the intended packages are gone, then `rm -rf node_modules && npm install` and re-run `next build` to catch any transitive-dependency surprise (the `date-fns` finding in §2.4 shows this repo already has one silent transitive dependency)
7. For Batch 3 (speed-test route): manually load `/interview/[token]/setup` in a dev server and confirm the connection-speed check still works end-to-end via `@cloudflare/speedtest` (i.e., confirm the deleted route truly isn't on any code path, not just statically unreferenced)

## 6. Rollback note per batch

All five batches are plain `git revert <commit>` — no data migrations, no schema changes, nothing stateful. Batch 1's `package-lock.json` regeneration is the only step that isn't a pure file deletion; if a rollback is needed there, restore both `package.json` and `package-lock.json` from the pre-batch commit and re-run `npm install` rather than hand-editing the lockfile back.

---

## Which batch would you like to start with?
